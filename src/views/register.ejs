<!DOCTYPE html>
<html lang="es">

<%- include('partials/head', {title: "Registro"}) %>

<body>
<%- include('partials/header') %>    
    <main class="register">
        <div class="register__box">
            <h2>registrate </h2>
            <form action="" class="register__form" method="post" enctype="multipart/form-data">
                <div>
                    <label class="form-label" for="name">Nombre</label>
                    <input class="form-control" id="name" type="text" name="name" placeholder="Ej: Eric" value="<%=  locals.old && (old.name && old.name ) %>">
                    <small class="text-danger ms-2"><%= locals.errors && (errors.name && errors.name.msg) %></small>
                </div>
                <div>
                    <label for="surname" class="form-label">Apellido</label>
                    <input class="form-control" id="surname" type="text" name="surname" placeholder="Ej: Mena" value="<%=  locals.old && (old.surname && old.surname ) %>">
                    <small class="text-danger ms-2"><%= locals.errors && (errors.surname && errors.surname.msg) %></small>
                </div>
                <div id="register__form--email">
                    <label class="form-label" for="email">Email</label>
                    <input class="form-control" id="email" type="email" name="email"    placeholder="Eric_BATMAN@formar.com" value="<%=  locals.old && (old.email && old.email ) %>">
                    <small class="text-danger ms-2"><%= locals.errors && (errors.email && errors.email.msg) %></small>
                </div>
                <div>
                    <label class="form-label" for="password">Contraseña</label>
                    <input class="form-control" id="password" type="password" name="password" >
                    <small class="text-danger ms-2"><%= locals.errors && (errors.password && errors.password.msg) %></small>
                </div>
                <div>
                    <label class="form-label" for="password2">Confirmar Contraseña</label>
                    <input class="form-control" id="password2" type="password" name="password2">
                    <small class="text-danger ms-2"><%= locals.errors && (errors.password2 && errors.password2.msg) %></small>
                </div>
                <div>
                    <label for="form-label" for="image" >Agrega tu avatar</label>
                    <input type="file" name="img" id="image" class="form-control">
                </div>
                <div class="register__form--btn">
                    <button type="submit" class="register__form--btn--crear">crear cuenta</button>
                    <p>¿Ya tenés cuenta? <a href="/users/login"> inicía sesión</a></p>
                </div>
            </form>
        </div>
    </main>
<%- include('partials/footer') %>
<%- include('partials/scripts') %>

</body>

</html>